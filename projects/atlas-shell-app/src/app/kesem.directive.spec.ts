import { KesemDirective } from './kesem.directive';

describe('KesemDirective', () => {
  it('should create an instance', () => {
    const directive = new KesemDirective();
    expect(directive).toBeTruthy();
  });
});
