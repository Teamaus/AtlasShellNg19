import { Show3Directive } from './show3.directive';

describe('Show3Directive', () => {
  it('should create an instance', () => {
    const directive = new Show3Directive();
    expect(directive).toBeTruthy();
  });
});
