import { WfCurrentDirective } from './wf-current.directive';

describe('WfCurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new WfCurrentDirective();
    expect(directive).toBeTruthy();
  });
});
