import { WfDirective } from './wf.directive';

describe('WfDirective', () => {
  it('should create an instance', () => {
    const directive = new WfDirective();
    expect(directive).toBeTruthy();
  });
});
