import { WfIfDirective } from './wf-if.directive';

describe('WfIfDirective', () => {
  it('should create an instance', () => {
    const directive = new WfIfDirective();
    expect(directive).toBeTruthy();
  });
});
