import { WfForDirective } from './wf-for.directive';

describe('WfForDirective', () => {
  it('should create an instance', () => {
    const directive = new WfForDirective();
    expect(directive).toBeTruthy();
  });
});
