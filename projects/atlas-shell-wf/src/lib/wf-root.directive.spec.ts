import { WfRootDirective } from './wf-root.directive';

describe('WfRootDirective', () => {
  it('should create an instance', () => {
    const directive = new WfRootDirective();
    expect(directive).toBeTruthy();
  });
});
