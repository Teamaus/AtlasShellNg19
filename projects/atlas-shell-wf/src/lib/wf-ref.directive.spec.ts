import { WfRefDirective } from './wf-ref.directive';

describe('WfRefDirective', () => {
  it('should create an instance', () => {
    const directive = new WfRefDirective();
    expect(directive).toBeTruthy();
  });
});
