import { WfCollectionDirective } from './wf-collection.directive';

describe('WfCollectionDirective', () => {
  it('should create an instance', () => {
    const directive = new WfCollectionDirective();
    expect(directive).toBeTruthy();
  });
});
