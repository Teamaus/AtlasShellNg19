import { WfStepDirective } from './wf-step.directive';

describe('WfStepDirective', () => {
  it('should create an instance', () => {
    const directive = new WfStepDirective();
    expect(directive).toBeTruthy();
  });
});
