import { WfIteratorDirective } from './wf-iterator.directive';

describe('WfIteratorDirective', () => {
  it('should create an instance', () => {
    const directive = new WfIteratorDirective();
    expect(directive).toBeTruthy();
  });
});
