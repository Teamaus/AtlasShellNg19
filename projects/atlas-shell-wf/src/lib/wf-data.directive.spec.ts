import { WfDataDirective } from './wf-data.directive';

describe('WfDataDirective', () => {
  it('should create an instance', () => {
    const directive = new WfDataDirective();
    expect(directive).toBeTruthy();
  });
});
