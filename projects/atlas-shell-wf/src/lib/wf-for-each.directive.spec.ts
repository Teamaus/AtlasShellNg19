import { WfForEachDirective } from './wf-for-each.directive';

describe('WfForEachDirective', () => {
  it('should create an instance', () => {
    const directive = new WfForEachDirective();
    expect(directive).toBeTruthy();
  });
});
