import { AtlasShellSubscriber } from './atlas-shell-subscriber';

describe('AtlasShellSubscriber', () => {
  it('should create an instance', () => {
    expect(new AtlasShellSubscriber()).toBeTruthy();
  });
});
