import { AtlasShellPublisher } from './atlas-shell-publisher';

describe('AtlasShellPublisher', () => {
  it('should create an instance', () => {
    expect(new AtlasShellPublisher()).toBeTruthy();
  });
});
