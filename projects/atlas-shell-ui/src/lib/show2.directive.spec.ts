import { Show2Directive } from './show2.directive';

describe('Show2Directive', () => {
  it('should create an instance', () => {
    const directive = new Show2Directive();
    expect(directive).toBeTruthy();
  });
});
