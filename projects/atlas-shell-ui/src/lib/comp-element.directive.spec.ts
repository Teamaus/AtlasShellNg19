import { CompElementDirective } from './comp-element.directive';

describe('CompElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CompElementDirective();
    expect(directive).toBeTruthy();
  });
});
