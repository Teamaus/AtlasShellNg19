import { AtlasReuseStrategy } from './atlas-reuse-strategy';

describe('AtlasReuseStrategy', () => {
  it('should create an instance', () => {
    expect(new AtlasReuseStrategy()).toBeTruthy();
  });
});
