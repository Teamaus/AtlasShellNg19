import { AtlasShellRootDirective } from './atlas-shell-root.directive';

describe('AtlasShellRootDirective', () => {
  it('should create an instance', () => {
    const directive = new AtlasShellRootDirective();
    expect(directive).toBeTruthy();
  });
});
